# Digital-Marketing-Performance-Analysis-MACHINE-LEARNING
Classification (booking prediction) and regression (revenue forecasting) models built with proper time-based train/test splits and leakage-free features, evaluated against naive baselines — including the transparent finding that the models don't beat "predict the average."
